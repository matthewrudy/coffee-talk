# single line
sayOnce = (word) -> word

# multilines
sayThrice = (word) ->
  word +
  word +
  word
  
# with no arguments
fortyTwo = -> 42

# with no body
blackHole = (input)->

# with nothing
nothing = ->